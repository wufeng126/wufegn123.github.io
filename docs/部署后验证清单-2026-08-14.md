# 部署后验证清单（2026-08-14 修复批次）

> 适用提交：`d73b29c`（审查修复批次）、`a4735ae`（D4/D2 数据口径）、`2585ab3`（财务核心单测）
> 用途：生产/预发环境部署后，逐项验证本次改动未引入回归、且修复生效。

## 一、部署方式确认（先填）

- [ ] 部署平台：□ Coze 平台自动部署 □ 手动构建（`pnpm build` + `pnpm start`）□ 其他：______
- [ ] 部署目标：□ 生产（sxshhy.top） □ 预发/测试
- [ ] 数据库：□ Supabase（需确认 service_role key 已配置） □ 其他

> ⚠️ 若使用 Coze 平台部署，构建产物中的 `.babelrc.js` 与 `MIGRATION_SQL`（含 35 张核心表建表）会随启动自动生效；确认部署日志出现
> `[DB Migration] ...` 且无报错。

## 二、分项验证

### 1. 通知已读权限（修复 S7 过度回归）
- [ ] 用**普通权限用户**（仅有 notifications:view，无 notifications:settings）登录
- [ ] 通知铃铛打开 → 点"全部已读" → 应成功，不再出现 403
- [ ] 通知中心单条标记已读 → 成功
- [ ] 通知设置页面（修改 Webhook 等）仍需要 settings 权限 → 普通用户应被拒绝（符合预期）

### 2. 生产构建模式（.babelrc.js）
- [ ] 构建日志出现 `Using external babel configuration from ...\.babelrc.js`（而非 .babelrc）
- [ ] 构建耗时/产物体积较上版有下降（预期：React 生产模式 + 无 inspector 插件）
- [ ] 抽查页面交互（如 React 组件正常渲染、无"development mode"警告）

### 3. 核心表建表迁移（A1，幂等）
- [ ] 生产启动日志：`[DB Migration]` 成功信息，无 SQL 报错（老库已存在表 → no-op）
- [ ] （可选）在 Supabase SQL Editor 手动执行 `migrations/create_core_tables.sql`，应全部成功、无重复键冲突
- [ ] （新环境验证）全新数据库部署后，登录/项目/工人/工资等核心功能可正常读写

### 4. 项目权限合并（A4）
- [ ] 管理员在"系统管理 → 权限管理"给某用户配置了**项目角色**（预算员/项目经理等）且该用户 `managed_projects` 未手动填写
- [ ] 该用户登录 → 花名册、月度工资应能看到授权项目的数据（此前查不到）
- [ ] 超级管理员行为不变：仍可见全部数据

### 5. alert() → toast()
- [ ] 打开 证据链（evidence-chain）→ 不填项目直接保存 → 应弹出 toast 错误而非浏览器 alert
- [ ] 打开 投标测算-测算中心/历史标段库 → 触发校验错误/导入失败 → toast 提示
- [ ] 全站搜索确认无残留 alert：`grep -rn "alert(" src/` 应为空

### 6. ui-preview 不再公开
- [ ] 未登录访问 `/ui-preview` 或 `/ui-preview/xxx` → 被引导到登录页（不再直接展示）
- [ ] 登录后超级管理员仍可访问（供设计预览使用）

### 7. 死代码 CSS 删除
- [ ] 登录页、工作台、月报、表格、弹窗等视觉无回归（布局/颜色/圆角正常）
- [ ] 控制台无 CSS 相关报错（如未定义类不产生错误）

### 8. Lint 门禁
- [ ] 类 Unix 环境：`pnpm validate` 通过（注意：`--parallel '/^(ts-check|lint:build)$/'` 正则过滤在 Windows 的 cmd/pwsh 下不兼容，用 `pnpm ts-check` + `pnpm lint:build` 代替）
- [ ] `pnpm test`：6 个测试文件 80 用例全部通过

### 9. D4/D2 数据口径（重点，需业务数据）
- [ ] 打开 月度经营月报 → 选择一个结算量大的项目 → 供应商结算金额 与 供应商台账（合同管理→台账）应一致（此前可能重复计）
- [ ] 打开 首页 → 成本构成"材料机械"金额 与 成本利润中心"供应商结算"金额 应一致（此前 dashboard 只算老表、成本中心只算新表）
- [ ] 抽查：同一笔结算若同时存在于新旧两表（若有迁移历史），报表只计一次
- [ ] 作废的结算单不应计入任何口径

### 10. useConfirm 统一确认
- [ ] 任一删除操作（工人/供应商/合同/工资/结算/签证附件等）→ 弹出统一确认框：红色"确认"按钮 + 取消
- [ ] Esc 键 / 点击遮罩 → 取消；点击确认 → 执行
- [ ] 原 confirm() 文案内容完整呈现（主问句 + 后果说明）
- [ ] 移动端弹窗全屏显示正常（复用全局 dialog 样式）

## 三、回滚预案

- 若某项验证失败：先定位到具体提交。
  - `d73b29c` 含权限/构建/迁移/UI 改动（可整体 revert）
  - `a4735ae` 仅 D4/D2 口径（revert 后口径回到改动前行为）
  - `2585ab3` 仅新增测试文件（无运行时代码影响，无需回滚）
- 数据库迁移幂等，无需回滚操作；如担心建表影响，可仅在生产**不执行** `create_core_tables.sql`（自动迁移会执行，但 IF NOT EXISTS 安全）。

## 四、验证结论

| 项 | 结果 | 备注 |
|----|------|------|
| 1 通知已读 | □ 通过 □ 失败 | |
| 2 生产构建 | □ 通过 □ 失败 | |
| 3 建表迁移 | □ 通过 □ 失败 | |
| 4 项目权限 | □ 通过 □ 失败 | |
| 5 toast 替换 | □ 通过 □ 失败 | |
| 6 ui-preview | □ 通过 □ 失败 | |
| 7 CSS 回归 | □ 通过 □ 失败 | |
| 8 门禁/测试 | □ 通过 □ 失败 | |
| 9 数据口径 | □ 通过 □ 失败 | |
| 10 统一确认 | □ 通过 □ 失败 | |
